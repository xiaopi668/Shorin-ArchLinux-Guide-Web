# 笔记本优化

## 一、电源管理

GNOME / KDE 默认已带 power-profiles-daemon（桌面性能模式切换），日常使用已足够。**TLP 是可选增强**，与桌面自带方案不要同时用（会造成冲突），追求更省电再装：

```bash
sudo pacman -S tlp
sudo systemctl enable --now tlp
```

电池校准：

```bash
sudo tlp calibrate
```

查看状态：

```bash
sudo tlp-stat -s
```

### 1.1 电池充放优化

```bash
# 查看电池状态
cat /sys/class/power_supply/BAT0/status

# 查看电量
cat /sys/class/power_supply/BAT0/capacity
```

支持 charge-threshold 的笔记本（ThinkPad 等）可以限制充电上限保护电池，编辑 `/etc/tlp.conf`：

```ini
START_CHARGE_THRESH_BAT0=80
STOP_CHARGE_THRESH_BAT0=80
```

## 二、屏幕亮度

### 2.1 快捷键调节

GNOME / KDE 桌面的 Fn 亮度键一般开箱即用。无效时安装：

```bash
sudo pacman -S brightnessctl
brightnessctl set 50%     # 设 50%
brightnessctl set +10%    # 增加 10%
```

把命令绑定到 Fn 键（快捷键设置见[我的GNOME自定义设置](我的GNOME自定义设置.md)或[我的KDE自定义设置](我的KDE自定义设置.md)）。

### 2.2 命令行调节

```bash
# 列出可调设备
brightnessctl list

# 查看当前值
brightnessctl get
```

## 三、触摸板手势（libinput）

### 3.1 启用自然滚动与点击

GNOME 在系统设置 → 鼠标与触摸板中开启"点击触摸"与自然滚动。KDE 在 系统设置 → 输入设备 → 触摸板。

命令行方式，编辑 `/etc/X11/xorg.conf.d/30-touchpad.conf`（X11 环境）：

```ini
Section "InputClass"
    Identifier "touchpad"
    Driver "libinput"
    MatchIsTouchpad "on"
    Option "Tapping" "on"
    Option "NaturalScrolling" "true"
    Option "ClickMethod" "clickfinger"
EndSection
```

Wayland 下（GNOME/KDE 默认）系统设置即可完成，无需此文件。

### 3.2 三指 / 四指手势

- GNOME：系统设置 → 鼠标与触摸板 → 三指手势（任务切换、概览）
- KDE：系统设置 → 输入设备 → 触摸板 → 手势

Wayland 下的高级自定义手势可以装 touchegg（官方源）：

```bash
sudo pacman -S touchegg
```

并安装对应桌面扩展（GNOME 的 "Touché" 或 KDE 的 "Touchégg KDE"）来配置手势。

> 提示：`libinput-gestures` 需要 X11 会话，Wayland 下直接用桌面自带手势即可。

## 四、Fn 功能键

部分笔记本 Fn 键与媒体键颠倒（默认 Fn+F1~F12 是媒体键，或反过来）。

常见做法：
- ThinkPad：`Fn+Esc` 切换 FnLock
- 多数笔记本 BIOS 里有 "Fn and Ctrl Key swap" / "Fn Lock" 选项

## 五、合盖行为

```bash
# 查看当前合盖设置
systemd-inhibit --list
```

编辑 `/etc/systemd/logind.conf`：

```ini
[Login]
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
```

合盖外接显示器时不想休眠，把 `HandleLidSwitchExternalPower` 改为 `ignore`。改完重启或 `sudo systemctl restart systemd-logind`。

## 六、CPU 性能模式

```bash
# 查看当前模式
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor

# 切换到省电 / 性能（需 amd_pstate 或 intel_pstate 支持）
echo powersave | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor
```

桌面环境（GNOME/KDE）都有图形化的性能模式切换，日常使用无需手动设置。
